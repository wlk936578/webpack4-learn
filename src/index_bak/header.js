export default function Header (dom) {
    let header = document.createElement('div')
    header.innerText = '头部'
    dom.append(header)
}

// module.exports = function Header (dom) {
//     let header = document.createElement('div')
//     header.innerText = '头部'
//     dom.append(header)
// }